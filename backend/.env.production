# Production Environment Variables for Free Deployment
# Copy this to .env in your backend folder before deploying

# Server Configuration
PORT=4000
NODE_ENV=production

# Database (Get from Neon.tech - FREE)
# 1. Sign up at https://neon.tech
# 2. Create a project
# 3. Copy the connection string
DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# JWT Secret (Generate a random string)
# Run this in terminal: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Frontend URL (Update after deploying frontend to Vercel)
FRONTEND_URL=https://your-app.vercel.app

# Optional: Redis (Leave empty to use in-memory fallback)
# REDIS_URL=

# Optional: Code Execution (Piston API - FREE, no signup needed)
PISTON_API_URL=https://emkc.org/api/v2/piston
